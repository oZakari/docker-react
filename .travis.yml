language: generic
sudo: required
services:
  - docker
  
before_install:
  - docker build -t ozakari/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ozakari/docker-react npm run test
